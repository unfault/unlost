# agents/

Vendor-specific integrations that make unlost "just work" without manual setup.

This directory is intentionally separate from core proxy/recording logic.

Current targets:
- `agents/opencode/`: OpenCode integration (plugin-first; no HTTP proxy required).
